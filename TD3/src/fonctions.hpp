#pragma once
#include <vector>
#include <algorithm>
#include <vector>
#include <string>
#include <iostream>

bool is_sorted(std::vector<int> const& vec);

// Exo 1
void bubble_sort(std::vector<int> & vec);
